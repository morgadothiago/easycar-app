import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#121212", // fundo elegante e moderno
    paddingHorizontal: 5,
    paddingTop: 16,
  },
});
